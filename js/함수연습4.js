window.onload = function(){
    function f(){
        let a = prompt("숫자를 입력하세요.");
        let b = prompt("숫자를 입력하세요.");
        let y = 0;
        for(let x = a; x <= b; a++){
            
        }
    }
}